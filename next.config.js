/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  // Day 1: no env validation. Day 2 will add @t3-oss/env-nextjs.
};

module.exports = nextConfig;
